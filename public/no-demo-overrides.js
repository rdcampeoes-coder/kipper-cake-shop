// Textos e ecrãs finais sem conceitos de demonstração/simulação.
more=function(){
  return `<div class="card settings-box"><h2 style="font-family:Georgia,serif;margin-top:0">Definições</h2><p class="muted">Configurações da tua conta e ferramentas do negócio.</p><div class="component-row"><div><strong>Dados da conta</strong><div class="muted small">Clientes, produtos, encomendas, stock e finanças pertencem apenas a este negócio.</div></div><span class="pill green">Isolados</span></div><div class="hidden-advanced"><details><summary>Avançado</summary><div style="padding-top:14px"><div class="danger-box"><strong>Limpar quantidades das receitas</strong><br><span class="small">Mantém produtos, componentes e ingredientes, mas remove quantidades e pesos de referência.</span></div><button class="danger" style="margin-top:12px" data-reset>Limpar dados das receitas</button></div></details></div></div>`;
};

compList=function(){
  const groups=[...new Set(data.components.map(c=>c.component_group))];
  return `<div class="section-head"><div><h2>Componentes</h2><div class="muted small">Cria as tuas próprias massas, recheios, coberturas e respetivas proporções.</div></div><button class="primary" data-new-component>+ Novo componente</button></div><div class="grid cards-2">${groups.map(g=>`<div class="card"><h3 style="font-family:Georgia,serif">${g}</h3>${data.components.filter(c=>c.component_group===g).map(c=>`<div class="component-row" style="padding:10px 0;border-bottom:1px solid var(--line)"><div><strong>${c.name}</strong><div class="muted small">${c.recipe?.length||0} ingredientes · ${Number(c.reference_weight)||0} g referência</div></div><div class="actions"><button class="secondary" data-component-edit="${c.id}">Editar</button><button class="ghost" data-delete="components" data-id="${c.id}">🗑</button></div></div>`).join('')||'<div class="empty">Ainda não existem componentes.</div>'}</div>`).join('')||'<div class="card empty">Ainda não existem componentes. Usa “Novo componente” para começar.</div>'}</div>`;
};
